@AccessControl.authorizationCheck: #NOT_REQUIRED
@EndUserText.label: 'PO Item App'
//@Metadata.ignorePropagatedAnnotations: true
@Metadata.allowExtensions: true
define view entity YC_POITM as projection on YI_POITM
{
    key PONumber,
    key ItemNumber,
    Description,
    Qty,
    UnitPrice,
    TotalAmt,
    LocalCreatedBy,
    LocalCreatedAt,
    LocalLastChangedBy,
    LocalLastChangedAt,
    LastChangedAt,
    /* Associations */
    _Ekko : redirected to parent YC_POHDR
}
